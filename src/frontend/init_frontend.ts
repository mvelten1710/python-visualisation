import { ExtensionContext } from "vscode";
// import { FrontendSession } from "./frontend_session";
// import { VisualizationPanel } from "./visualization_panel";

export async function initFrontend(trace: BackendTrace, context: ExtensionContext) {
    // const frontendSession = new FrontendSession(); 
    // const frontendTrace = await frontendSession.generateFrontendTrace(trace);
    //VisualizationPanel.render(context);
}